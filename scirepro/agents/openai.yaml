interface:
  display_name: "SciRepro"
  short_description: "研判科研图件复现路线，并交付可验证、可追溯的复现结果"
  default_prompt: "Use $scirepro to hand semantic schematics off to $sci-diagram-pptx immediately, installing the pinned companion if absent; assess retained figures in a local report before reproduction."
